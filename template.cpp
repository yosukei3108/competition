#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

// 計算量の目安: 実行時間制限が 1 秒の場合
// 1,000,000 -> 間に合う
// 10,000,000 -> 概ね間に合う
// 100,000,000 -> 間に合わない可能性が高い

int main(void){
    int n, m;
    string s;
    cin >> n >> m >> s;
    
    int x[100];
    for(int i=0; i<n; i++){
        cin >> x[i];
    }
    
    int ans = 0;
    
    cout << ans << endl;
    return 0;
}
